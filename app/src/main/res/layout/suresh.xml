<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="suresh"
            type="com.example.socialconnectapp.Suresh" />
    </data>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginTop="20dp"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@+id/availability"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="@{suresh.name}"
        tools:text="Suresh"
        android:textSize="25dp"
        android:textColor="#000"/>

    <ImageView
        android:id="@+id/availability"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@{suresh.availability ? @mipmap/ic_launcher_round :@drawable/ic_launcher_background}"
        tools:src="@drawable/ic_launcher_background"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>