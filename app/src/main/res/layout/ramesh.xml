<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="ramesh"
            type="com.example.socialconnectapp.Ramesh" />
    </data>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginTop="20dp"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@+id/availability"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="@{ramesh.name}"
        tools:text="Ramesh"
        android:textSize="25dp"
        android:textColor="#000"/>

    <TextView
        android:id="@+id/emailId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/name"
        app:layout_constraintStart_toEndOf="@+id/availability"
        android:text="@{ramesh.emailId}"
        tools:text="ramesh.kumar@gmail.com"
        android:textSize="25dp"
        android:textColor="#000" />

    <ImageView
        android:id="@+id/availability"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@{ramesh.availability ? @mipmap/ic_launcher_round :@drawable/ic_launcher_background}"
        tools:src="@mipmap/ic_launcher_round"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>